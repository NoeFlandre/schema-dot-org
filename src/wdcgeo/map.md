## Where the records are

![Density of the records over the world]({map_image})

Every record in this dataset, counted into quarter-degree cells and shaded on a
logarithmic scale, in an equirectangular projection. No coastline is drawn: the
outlines are the data, and the empty regions are as much a property of this
corpus as the dense ones.

